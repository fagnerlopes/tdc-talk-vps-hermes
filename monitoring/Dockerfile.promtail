# Fallback do R3 — ver monitoring/Dockerfile.loki.
#
#   build: { context: ./monitoring, dockerfile: Dockerfile.promtail }
#   volumes: [- app_logs:/var/log/app:ro, - promtail_positions:/promtail]
FROM grafana/promtail:3.1.1
COPY promtail-config.yaml /etc/promtail/config.yml
